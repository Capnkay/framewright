{
  "name": "framewright-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Framewright client — React app hosting the generated CMS sections and the Generator Studio. Scaffolded in Phase 1 (see ../docs/ROADMAP.md); these dependencies are declared now so Phase 1 can install them, but nothing in ../tests/ requires them to be present.",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-redux": "^9.1.2",
    "@reduxjs/toolkit": "^2.2.7",
    "react-router-dom": "^6.26.1",
    "primereact": "^10.8.3"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  }
}
