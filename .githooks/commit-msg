#!/bin/sh
# .githooks/commit-msg
#
# DISABLED 2026-08-24 — team decision, hours from deadline, same reason as
# .githooks/pre-commit (see there and docs/corrections/REGISTER.md,
# 2026-08-24). This hook required every commit message to contain a task id
# matching T-[0-9]{3}. Restore from git history if there's time after
# submission: `git log -p -- .githooks/commit-msg`.
exit 0
