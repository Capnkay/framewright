#!/bin/sh
# .githooks/pre-commit
#
# DISABLED 2026-08-24 — team decision, hours from deadline: this hook (secret
# scan, LAW-MANIFEST.sha256 integrity check, active-claim check) was blocking
# real commits and no one had time left to debug why on every machine it hit.
# See docs/corrections/REGISTER.md, 2026-08-24, for the full log and what is
# no longer checked. Restore from git history if there's time after
# submission: `git log -p -- .githooks/pre-commit`.
exit 0
