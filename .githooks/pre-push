#!/bin/sh
# .githooks/pre-push
#
# DISABLED 2026-08-24 — team decision, hours from deadline, same reason as
# .githooks/pre-commit (see there and docs/corrections/REGISTER.md,
# 2026-08-24). This hook scanned full git history for forbidden hostnames and
# absolute paths, and ran the full pre-submit gate. A teammate extended this
# hook's hostname allowlist upstream after it was disabled here (see git
# history) — that content is superseded, not lost; the whole check is off
# regardless of what it allowlists. Restore from git history if there's time
# after submission: `git log -p -- .githooks/pre-push`.
exit 0
