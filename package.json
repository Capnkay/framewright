{
  "name": "framewright",
  "private": true,
  "version": "0.1.0",
  "description": "Framewright \u2014 wireframe, code, or prompt to a CMS-ready React section, with a live preview and store-backed editing.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/**/*.mjs",
    "build": "echo \"no build step: docs/html is static\"",
    "vercel-build": "echo \"no build step: docs/html is static\""
  }
}
